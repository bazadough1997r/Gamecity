import { EDIT_PROFILE } from '../actions'


export let reducers = (state, action) => {
    switch (action.type) {
        case EDIT_PROFILE:
            break;
        //case :
            //break;
        //case :
            //break;

        default:
            return state
    }
}